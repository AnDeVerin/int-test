export enum ButtonType {
  Primary = 'primary',
  Secondary = 'secondary',
  Flat = 'flat',
  Destructive = 'destructive',
}

export enum ButtonSize {
  Small = 's',
  Large = 'l',
}

export enum ButtonIconPosition {
  Left = 'left',
  Right = 'right',
  Only = 'only',
}
